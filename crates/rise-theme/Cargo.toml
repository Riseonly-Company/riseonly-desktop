[package]
name = "rise-theme"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
anyhow.workspace = true
serde.workspace = true
gpui.workspace = true
rise-core.workspace = true
# For Material and MaterialBacking only. rise-platform owns the POLICY - which
# materials are real on this machine - and this crate owns their painted form,
# so the vocabulary has to be shared. The edge points this way because
# rise-platform must never learn a colour; see docs/PLATFORM_GLASS.md.
rise-platform.workspace = true

[dev-dependencies]
serde_json.workspace = true
