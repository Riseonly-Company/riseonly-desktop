================================================================================
rise-chat
================================================================================

READ FIRST
  ../CONTEXT.txt      (the module convention — layout, store triad, hard rules)
  ../../../CONTEXT.txt (workspace rules)

REFERENCE
  ../../../riseonly-ios/Riseonly/modules/chat/
  125,508 lines of Swift in the reference.

FOLDERS HERE
  components, engine, pages, shared, stores, widgets

  This module owns a backend service, so it has engine/: the domain, the actor
  repositories, the endpoint catalogue and the presentation snapshots.

NOTES
  The largest module in the reference by a wide margin: 125,508 lines, 36% of the app.
  Budget it as a project, not a task. Its list engine is largely replaced by
  gpui's `list` (variable heights, bottom alignment, splice anchoring).

REMINDERS
  pages/ holds screens and nothing else.
  A view calls only <Feature>InteractionsStore.
  This crate must not depend on another module crate.
