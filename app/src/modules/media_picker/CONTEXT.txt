================================================================================
rise-media-picker
================================================================================

READ FIRST
  ../CONTEXT.txt      (the module convention — layout, store triad, hard rules)
  ../../../CONTEXT.txt (workspace rules)

REFERENCE
  ../../../riseonly-ios/Riseonly/modules/media-picker/
  17,376 lines of Swift in the reference.

FOLDERS HERE
  api, editor, export, library, permissions, selection, send, shared, ui

  No engine/ here. This module's data belongs to another module's domain, or
  it has no backend service of its own. Do not add engine/ without checking
  which backend service owns the data.

NOTES
  17,376 lines in the reference, built on Photos/PhotosUI/CoreImage. Desktop has
  no counterpart: open decision 4 is whether we ship an editor at all or a file
  dialog plus server-side transform.

REMINDERS
  pages/ holds screens and nothing else.
  A view calls only <Feature>InteractionsStore.
  This crate must not depend on another module crate.
