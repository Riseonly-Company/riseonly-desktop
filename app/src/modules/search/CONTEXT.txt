================================================================================
rise-search
================================================================================

READ FIRST
  ../CONTEXT.txt      (the module convention — layout, store triad, hard rules)
  ../../../CONTEXT.txt (workspace rules)

REFERENCE
  ../../../riseonly-ios/Riseonly/modules/search/
  4,092 lines of Swift in the reference.

FOLDERS HERE
  components, engine, pages, stores

  This module owns a backend service, so it has engine/: the domain, the actor
  repositories, the endpoint catalogue and the presentation snapshots.

REMINDERS
  pages/ holds screens and nothing else.
  A view calls only <Feature>InteractionsStore.
  This crate must not depend on another module crate.
